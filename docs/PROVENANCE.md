# 来源与处理说明

## 规范源

仓库 `books/` 中的 17 册 Markdown 和 `books/assets/` 来自已完成结构重建、公式校订、插图恢复、脚注整理及 EPUB 重排审计的 GPT 工作目录输出。

导入仓库时完成以下核对：

- Markdown：17 册
- 图片资源：4,981 个
- 图片引用：4,984 次
- 缺失引用：0
- 孤立资源：0
- 初次复制字节哈希不一致：0

## 仓库化修正

导入后只对一处会被 Pandoc 误解析的 Markdown 图片替代文本作了语法修正：

- 《代数（第三册）》图 2·12：将替代文本中的未转义方括号改为语义等价的“左闭右开区间（含 a，不含 b）”。

该修正确保 `assets/algebra-3/fig-p0047-03.png` 作为图片而不是错误超链接进入 EPUB。原导入文件已在仓库外保留备份。

## 原始扫描 PDF

仓库 `raw/` 保存17册导入时的 PDF 原字节，供校勘、缺页核对和重建复现使用。导入时完成：

- 17册、5,927页、134,941,542字节；
- 源文件与仓库副本 SHA-256 逐书一致；
- qpdf 结构检查17/17通过；
- 无加密、附件、批注、JavaScript、Launch动作、XFA或表单数据；
- 未发现本机路径、用户/主机标识、邮箱、手机、身份证、凭据或密钥；
- `raw/SHA256SUMS.txt` 固定逐书导入哈希。

部分文件包含 PDF-XChange 的版本、处理日期和校验摘要，以及普通 PDF 创建/修改日期、工具名和文档随机ID；这些字段不含用户或设备身份。PDF 未为“清理元数据”而重写，以保持 raw 字节可验证。

## 未纳入仓库的材料

为控制仓库范围，下列过程性材料未复制到此处：

- OCR 逐页原始输出
- 中间裁图和视觉抽检截图
- 历史备份目录
- 候选与生产 EPUB 的临时构建目录

这些材料不属于规范电子书源。
